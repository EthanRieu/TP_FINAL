meta {
  name: Update Product
  type: http
  seq: 4
}

patch {
  url: {{baseUrl}}/api/products/{{productId}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{sellerToken}}
}

body:json {
  {
    "title": "Montre vintage Seiko - édition limitée",
    "condition": "like_new"
  }
}
