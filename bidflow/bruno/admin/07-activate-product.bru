meta {
  name: Activate Product
  type: http
  seq: 7
}

patch {
  url: {{baseUrl}}/api/admin/products/{{productId}}/activate
  body: none
  auth: bearer
}

auth:bearer {
  token: {{moderatorToken}}
}
